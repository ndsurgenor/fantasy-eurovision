{% extends 'base.twig' %}

{% block title %}Coming Soon — {{ site_name }}{% endblock %}

{% block content %}
    <div class="text-center py-20">
        <h1 class="text-4xl font-bold text-yellow-400 mb-4">{{ site_name }}</h1>
        <p class="text-indigo-300 text-lg">{{ message }}</p>
    </div>
{% endblock %}
